# Dizer a área da parede

# Pedir altura e lagura
alt = float(input('Altura da parede:'))
lag = float(input('Largura da parede:'))
# Calcular a área
area = alt * lag / 2
# Dizer a área
print(f'A área é {area}')
